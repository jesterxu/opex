{% extends "index.html" %}
{% block ititle %}{{ category.chef_title }} Articles Category{% endblock %}
{% block metas %}
		<meta name="description" content="{{ category.description_chef }}">
		<meta name="keywords" content="halit, alptekin, programlama, blog, personal blog, kisisel blog, halit alptekin, linux, python, django, php, bootstrap, seo, bilgisayar muhendisligi, ktu, karadeniz teknik universitesi, programming, {{ category.chef_title|lower }}">
{% endblock %}
{% block icontent %}
		<div class="alert alert-block">
			<h4 class="alert-heading">{{ category.description|title }}</h4>
			<span>There are {{ makaleler.count }} articles in this category.</span>
		</div>					
		{% load pagination_tags %}
		{% autopaginate articles 5 %}
		{% for article in articles %}
			<h1><a href="{{ article.get_absolute_url }}">{{ article.title|title }}</a> <small>{{ article.created|date:"d F Y" }}</small></h1> 
			<p class="lead"> {{ article.description|title }}</p>
			{{ article.content_home|safe }}
			<div class="span offset7"><a href="{{ article.get_absolute_url }}" class="btn btn-medium btn-info" >Read More</a></div>
			<br>
			<hr>
		{% endfor %}
		{% paginate %}
{% endblock %}	
{% block javascriptler %}
<script src="/static/js/jquery.js"></script>
<script src="/static/js/bootstrap-alert.js"></script>
{% endblock %}		

